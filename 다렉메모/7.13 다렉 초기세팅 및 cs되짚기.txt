- 다렉x란? 그래픽카드에게
화면에 렌더링 하는 기능을 제공해주는놈

프젝 생성
빈 솔루션
(다음) -> 
이번에는 라이브러리화 시켜놓고 끌어다 쓰는 방식으로 만들것이다.


- lib 정적 dll 동적 (라이브러리 -> 코드)
  ##둘의 차이?? 
정적은 링크할때 실행파일에 포함을 시킨다.
동적은 실행파일을 포함시키지는 않는다. 그냥 끌어다 쓸 수 있게정도만.

# 함수의 구현부분이 lib로 만들어짐
lib는 바이너리코드라서 못봄(리버스엔지니어링 하면 볼 수는 있음)


이 코드를 연결한다 -> 링크한다.
링크를 하면 거깄는 라이브러리를 사용할 수 있게된다는 의미


- 컴파일 하는 이유 고수준->저수준 해서 컴퓨터 알아보게 하기 위해
저수준->어셈블리
기계어로 번역된게 exe

코드 // 코드가 올라가는 영역
데이터// 전역변수, 정적변수 // 프로그램 끝날때까지 유지
힙 // 동적할당 //반드시 제거를 해주어야한다.(릭)
스택 // 매개변수 지역변수 // 임시로 저장하는 공간이다.

프로그램 실행 -> 기계어 코드가 메모리(코드영역)에 올라온다.
-> 이 코드를 cpu가 연산해준다. -> 그래서 함수포인터는 하나뿐이다.(코드영역에서 가져오니까)

///bat

bat 파일: 도스파일을 실행시켜줌
파일을 복제해주는 명령어
xcopy 도스 복사 명령어

컴파일 후 빌드가 끝나면 이 배치파일이 작동됙  설계
$call 명령어
.\ 현재폴더
*.h 모든 헤더를 복사하겠다

