로딩창 진행률 시스템

fseek ftell? 파일크기 구하는 함수?

SEEK_SET 파일 커서 위치를 처음으로 되돌린다.

SceneInfo Load를 int타입으로 읽어오게 해서
크기를 감지하게 재설계

int 리턴 이 아니더라도 CurLoad 같은거 만들어서 하는 방법도 있음

//2교시

스레드 매니저에서 특정스레드 정지, 일시정지 기능 구현

■ 타일맵
나중에논 primitive 2d, 3d로 나뉠건데

sprite는 2d 전용 컴포넌트이다.

타일맵도 2D 컴포넌트의 종류이다.

타일도 낱장, 통으로 되어있는 경우가 있다.
타일 이미지는 무조건 하나(아틀라스)로 할 것이다.
->드로우콜을 하나로 하기 위해 하드웨어 인스턴싱을 사용할것


인스턴싱 조건

애니메이션 Array방식,
조건: 가로와 세로 모두 동일해야한다.
메시, 쉐이더도 동일해야한다.


■타일 맵 컴포넌트
타일 맵들을 모아둠

타일 객체가 필요