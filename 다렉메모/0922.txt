0922

픽셀충돌

이미지는 무조건 사각형

1. 충돌이 들어왔는지 검사
2. 픽셀 하나하나 검사(이 방법 말고는 없다.)
	최적화가 가능하지만 부하가 많은 연산.

무시하고 싶은 색상(투명)을 하나 추가해서 충돌처리하는 방법
//픽셀충돌 필요하면 만들어봐라

픽셀충돌 준비물
 - 사각형 정보
 - 픽셀정보(총 갯수)

8비트
16비트(알파값이 없는)

32비트(알파 포함)(PNG)
픽셀 하나당 4바이트

(엔진인포)픽셀충돌 타입
지정한 색을 무시할건지	
지정한 색 확장할건지
알파를 무시할지
알파를 확장할건지

ID3D11Shader (디버깅 확인용)

RefCount 이후 구조체
	같은 픽셀들이면 정보를 일일히 저장할 필요가 없으니
	같은값을 넣게 만들어준다.

■ 픽셀충돌 클래스
픽셀메시, 픽셀쉐이더(픽셀검사용?) 들게 하고

//2교시
픽셀충돌 마저 구현

■ 픽셀충돌용
콜라이더 이미지 쉐이더

