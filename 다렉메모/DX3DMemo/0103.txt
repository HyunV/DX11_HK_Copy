디퍼드 렌더링 완료,

애니메이션 메시

///////////
백 페이스 컬링

본은 보일 필요가 없다(렌더 할 필요가 없다.)
본은 관절별로 [행렬 정보]로만 존재한다.

■ weight(가중치)
  스키닝 
  - 일반적인 본 애니메이션은 팔을 접을때 짤린다.
   이때 가중치를 주어 어느 본에 얼마나 힘을 주는지에 알아내서 처리한다.
   *fbx에 값들이 주어져있다.

■ 본 구조체
- 문자열 이름
- 트리구조(Depth)
- ParentIndex(부모 정보)
- 오프셋 행렬(fbx가 가지고 있음, 연산에만 쓰면 됨)
- 실제 본행렬
(느려서 컴퓨트 쉐이더 사용할 것)

내일은 스켈레톤 정보를 뽑아내고
애니메이션을 읽어내는 메시 제작