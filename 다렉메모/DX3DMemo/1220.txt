다이렉트 X 3D 시작

- 카메라 2D-> 3D로 변경


3d메시는
3d모델링 툴을 이용해서
sdmax(보편적) MAYA, 블렌더(무료)?

이정보(파일)를 가져다
정점정보, 인덱스정보 가져다 출력하는 방식으로 감

.fbx라는 파일로 불러서 사용함

fbx는 fopen으로 읽어들이는게 아니고
AutoDesk에서 제공하는fbx SDK 라이브러리를 이용

라이브러리 뜯어다 사용할 것

이름 붙여줌
debug dll, mdr

64bit dll dll mdr

라이브러리 링크, include


3d 메시 분류
일반 static 메시
애니메이션 메시로 분리해서 사용

■static 메시 필요기능
- fbx 파일 로딩 기능

* 애니메이션은 본(bone)정보도 같이 가져옴

■ 메시 폴더 추가

2교시
fbx 로더

인덱스 정보
가중치 정보

3d는 재질표현을 위해 조명이 꼭 필요
디퓨즈 주변광(basecolor)
앰비언트 환경광 (반사로 인한 빛 감쇄 효과)
스페큘러 정반사광(광택나는 곳)
immisive (자체적으로 빛을 발산하는 쇼과)
HDR 렌더링?
transparency (투명도)
shininess()
디퓨즈 텍스처(일반 텍스처)
범프 텍스처(울퉁불퉁 재질 표현 용 텍스처)
스페큘러 텍스처(가죽벨트는 광택 x 금속 갑옷은 광택 ㅇ)
이런 부위별로 효과를 주는 텍스처


물리기반 렌더링(PhysicalBased Rendering) 
-> 상위(레이트레이싱)


노말매핑, 범프매핑(울퉁불퉁한 느낌을 주는것처럼 주는 트릭)


■3d텍스처 표현 정보
2D때는 UV, POS만 필요했는데
3d는 이제 조명도 많이 필요

이를 위해 앞으로 Normal벡터(법선벡터) 많이 사용할 것

즉
Pos,
Normal,(점의 normal, 오브젝트가 위치하는 공간)
UV,
Tangent(bump x)
Binormal(bump y)


■바깥족 벡터는 컨테이너(큰 덩어리)
,서브셋(부위)로 구성

머리 컨테이너에서 대머리로 만들고 싶으면
머리카락 서브셋을 제거
