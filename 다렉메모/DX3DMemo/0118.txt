Ray (반직선)

■ 스카이박스

큰 구를 만든다.

우리 구조가 
백페이스 컬링을 사용하고 있다.

깊이 비교 연산
깊이값 1(제일 먼 값)
픽셀마다 깊이 비교가 일어나면서


■큐브텍스처
박스 전개도 모양인 텍스처
큐브 텍스처는 UV좌표가 아닌 UVW 이다.
-1~ 1사이의 값

이걸 큐브모양으로 조립하고 구체로 매핑하여
하늘을 표현한다.

■ SkySphere
씬마다 하나만 가질 수 있는 클래스
조명 필요 없으므로 G버퍼 필요없다
renderstate depth를 lessequal로 처리한다.