애니메이션 버퍼

ratio 비율?
행인덱스

사원수
아핀변환

다이렉트 x 에서 지원은 하는데
쉐이더에서 구면선형보간을 만들어야한다.

※ 쉐이더 키워드
in, out inout

hlsl은 포인터나 레퍼런스가 없기 때문에 저런 키워드를 가져다 쓴다.

인자를 지정하지 않으면 Default로 in 취급 한다.
out -> 값을 되돌려주는 연산에는 쓸 수 있지만(레퍼런스)
연산 자체로는 크래시가 난다.


둘다 되게 할거면 inout 키워드를 쓴다.

lerp(선형보간) a스케일과 b스케일을 n 비율로 보간한다.
QuternionLerp(구면선형보간)


내일은 인스턴싱작업,
애니메이션