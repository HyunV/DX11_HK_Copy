■ 버텍스버퍼 제작

■ 리소스 매니저 제작
저번이랑 똑같이 만드는데 리소스 만드는 방식이 조금 다름
리소스 폴더에 메시, 텍스처, 폰트, 사운드 등등이 들어간다.
	● 메시 매니저 제작
	● 텍스처 폰트, 사운드

만들어서 엔진에 인클루드 해서 사용

●메시 class (부모용)(순수가상해서 추상클래스로 만들어줌)
 스태틱 메시 (나무, 물체 등 움직이지 않는 메시들)(3D)
 애니메이션 메시(플레이어 같이 움직이는 메시들)(3D)

※ 어떤 메시든 간에 버텍스버퍼를 꼭 가져야된다.
   인덱스 버퍼는 있든 말든 상관없음

2D는 사각형에 텍스처 효과 입혀서 표현할거
정점정보나 인덱스 정보 넣어줘서 메시를 만든다.

	○ 스프라이트 메시
		원점을 기준으로 1x1 스프라이트 사각형
		원점을 중심으로 1x1 스프라이트 사각형

	○ 버텍스 버퍼 구조체
	＃버퍼가  버텍스버퍼, 인덱스버퍼 상수버퍼 등등 되게 많은데
	버퍼 만들어주는 기능을 제공해주는 애가 있다.(ID3D11Buffer)
	  - 사이즈(정점 크기)
	  - 카운트(정점 개수)
	  - 데이터(보이드) 정점정보 //뭘로 만들지 모르니까 보이드
	  - (인덱스 버퍼 한정 추가)- DXGI_FORMAT
* 버텍스버퍼는 일반적으로 하나지만
  3D쪽 가게되면 여러개 생길 수도 있다.
따라서 엔진인포에 이렇게 나눠준다.
//버텍스 버퍼용으로 사용될 구조체 //점 4개만 세팅
0 1
2 3

//인덱스 버퍼용으로 사용될 구조체
삼각형으로 쓸 인덱스 지정(0,1,3)

● VertexColor
	위치(벡터3), 색상정보 가지고 있는 정점
	우리는 3D틀로 2D를 만들기 때문에, 그리고 행렬을 곱하려고


※ 메시를 쪼개서 만드는 이유:
사람의 팔이 잘리거나(..) 3D 캐릭터 일 때 몸통은 갑옷재질(갑옷광원)
팔은 가죽재질인 등 메시의 효과를 입혀주는 재질이 달라질 수 있기 때문	

● 메시 컨테이너 ? (위상구조) 어떤 도형을 그려낼지를 결정
  ○ D3D11_PRIMITIVE_TOPOLOGY : 트라이앵글리스트 많이 사용함. 
	스트립은 한붓그리기(2D에선 거의 안씀)

● 메시 슬롯
  버텍스 버퍼, 인덱스버퍼 포인터 가지고 

메시 컨테이너 벡터 (인덱스버퍼 늘어날 수 있으니 벡터로)
메시의 최소값, 최대값을 표현하기 위해  min max 세팅도 해둔다.

enum class MeshType : unsigned char -> 이거 각각 1바이트로 사용하겠다는 의미
{

}

//버텍스버퍼 하나, 인덱스버퍼 0~1개를 생성
● CreateMesh(버텍스데이터 정점크기 갯수, 버퍼용도, 위상구조, 인덱스데이터, 인사이즈 인크기, 
인덱스버퍼 사용처, 포맷형식)
정점정보, 인덱스 정보를 직접적으로 넣어서 만드는 메시
{
메시 컨테이너 만들고 세팅해줌
}

자식클래스도 이용할 수 있게 protected로
createBuffer(버퍼타입(버텍스, 인덱스), 데이터, 사이즈 , 카운트, 용도, 버퍼 이중포인터)

D3D11_BUFFER_DESC
 전체크기 = 사이즈 x 카운트 = 용량
 용도 : 버텍스? 인덱스 여부
 다이나믹 버퍼이면 (ccpu접근이 되면) write만주고
스탠딩 버퍼이면 R/W 준다.

D3D11_SUBSOURCE_DATA BufferData = 버퍼 데이터 담는다?
디바이스 정보 필요,


//버텍스버퍼 [위치(12바이트)|컬러]
버퍼 출력될때 위치가 무조건 먼저 출력된다.
그래서 포인터 연산으로 처리할거
1바이트 포인터 만들고 정점 개수만큼 포문 돌려서

ex) 삼각형이면
[p|c|p|c|p|c]
포인터연산을 퐁해서
p에서 다음 p로 점프해서 받을수 있게 연산하는 식이다.

렌더
렌더는 컨텍스트 씀
//전체(모든 슬롯)그리기
//내가 원하는 슬롯만 그리기
IA가 붙어있는건 렌파라랑 관련있음

//메시가 다 만들어지면 
HLSL 을 이용해 셰이더로 출력해야됨