0818
트리 에디터 버그 수정
iso 17표준으로 잡는다

editorExclude 제작 ㅇ

엔진에 게임오브젝트 이동함.

엔진, 클라도 다 17표준으로 잡아놓기

cpp 제공하는 헤더중
filesystem이라는게 있ㅇ므

디렉토리 이터레이터(경로)
해놓으면 다 갖다 쓸 수 있게 만들어준다.

이러면 간단하게 for문으로 파일 이름들을 싹 긁어올 수 있다.

path에서 generic 뽑아서 c_str해서 일반문자열로 만들어줌

■ CDO 이론
class default object

디폴트 오브젝트를 만들어서 복사해서 쓰는 방식

//
트리 완성,

에디터 완성되면 그대로 클라에 복사해서 쓸 수 있게 구현

에디터 17표준으로 잡아놨는데
폴더에 들어있는 cpp h들을 통으로 분석하는 기능을 쓸 예정 

메뉴기능
Scene 저장(오브젝트, 컴포넌트 통째로 저장해서 클라이언트서도 그대로 갖다 쓸 수 있게 )
gameObject Empty
GameObject (게임 오브젝트를 불러와서 리스트박스 생성한다.)

오브젝트 생성기능, (윈도우 씬에 배치된 모든 오브젝트를 계층구조로 보여주는 식으로 만들 것)
컴포넌트 생성기능 윈도우 (오브젝트에 적용된 컴포넌트를 보여주고 트랜스폼도 조작할 수 있게 )


create window 창을 만들어서 생성할 수 있는 클래스 타입들을 다 보여줄 것
파일메뉴로도 생성할 수 있으나 버튼으로도 생성할 수 있게

##출력

에디터에서 empty 오브젝트 만들면  오브젝트 윈도우에서 생성되는걸 볼 수 있다.

