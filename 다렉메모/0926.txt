■ 마우스 콜리전 로직 마저 작업

■ 마우스와 각 모양별로 충돌

■ UI작업, 마우스와 UI 충돌
UI는 컴포넌트가 아닌 별도의 출력시스템으로 제작할것
방식은 에디터랑 비슷함(윈도우, 위젯 등)..
IMGUI가 아닌 만들어서 사용

- UI윈도우(CRef)
 엔진? 인클루드에 UI폴더 제작
 
에디터 매니저 처럼 만들건데
얘는 에디터GUI매니저같은 애가 필요없을듯?

그럼 누가 관리해줄것인가?

관리결정의 여부(씬 별로 관리 할것인가?, 전역적으로 관리할것인가)
에디터는 전역적(싱글톤)

씬별로 UI가 달라질 수 있으니
UI매니저는 씬별로 관리할 수 있게 설계하는게 효율이 좋다.

씬에 뷰포트 클래스를 만들어서 여기 UI를 처리

*UI특징
 2D, 최상위 화면
 따라서 UI는 깊이버퍼를 무시하고 무조건 앞에 출력할
 것이다. 따라서 마지막에 출력할것

//2교시
씬뷰포트 작업
뷰포트에는 윈도우 추가?
윈도우에서는 UI(위젯) 추가?

윈도우 기능(위치정보, 회전정보 고려)
-UI용 메쉬 제작
UI는 전부 사각형 Rect Bottom UV가지고 할 것.

위젯
m_pos 상대적인 정보(부모로부터 받은 정보?)
m_RenderPos 이게 진짜 위치?
...
opacity 불투명도?

출력용 쉐이더
출력용 메시

set쉐이더 만들어놓긴 했는데
UI에서 메시 바꿀일이 거의 없긴 함

출력처리만 잘 잡아놓으면 무난하게 완성 가능
(출력용이기에 상수버퍼도 필요하다.)

내일은 UI쉐이더, 상수버퍼
버퍼 이미지 세팅해서 화면에 출력해볼것

이후에는 각종 유틸위젯 제작할듯.