디라이트 사용법 말고는 딱히 어려운 부분 없음
※ 버그수정
- 프레임 형태로 저장했을 때 못불러오는 현상 수정
Frame은 owner가 지정이 안되었음

■새 위젯 추가
+ 폰트매니저,(폰트)

■ +텍스트 출력 위젯 작업?
다렉에서 텍스처 출력하려면 DWrite 라고 필요하다
따로 라이브러리를 빌드하는건 아니고

다렉도 2D시스템이 있긴 한데 표현력에 한계가 있어
그려낸 사각형에 그리는형식

##엔진에 라이브러리 추가(dwrite.h, d2d1.h)
디라이트는 d2d를 가지고 표현할 수 있게 해준다?

+링크도 필요
pragma comment

현재 출력은
11의 디바이스를 초기화해서 뷰포트에 담아서 출력하고 있다.

근데 디라이트는
D2D에다 출력을 해야한다.
D2D는 별도의 디바이스(백버퍼)가있다.

고로 D2D를 우리가 초기화한 백버퍼에 출력하게 만들어야 한다.

□구현과정

■ 2D RenderTarget
준비물:
	ID2D1RenderTarget 생성
	ID2D1RenderTarget 월드용 타겟 생성
	ID2D1Factory
	
	BGRA SUPPORT(얘가 반드시 있어야됨 D2D 에 쓸라고 예전에 만들어 놨던 것
	얘가 있어야 2D 렌더타겟이 백버퍼로 출력하는 시스템을 만들 수 있음)
	
	
1. 팩토리 생성(멀티스레드 타입, 팩토리 변수 주소)

2. 3D 백버퍼 surface 가져오기
※예를 들면 이미지 100x100 픽셀정보 저장하는 곳이 surface?

3. 스왑체인->getBuffer(0, 쓸 백버퍼 가져오기)

4. 2D용 렌더타겟 제작(m_2DFactory)
※ 출력하는 백버퍼를 3d 백버퍼로 지정해서 출력한다.
얘네는 3d 백버퍼에도 자동으로 출력될 것

준비물:

4-1. D2D1_RENDER_TARGET_PROPERTIES 

(1) 렌더타겟 타입: 하드웨어
(2) d2d1픽셀포멧(언노운, 알파모드:프리멀티파이드) // 고정값

4-2. 서페이스 렌더타겟 생성

레퍼런스 무조건 1 감소시킬 수 있게 작업 필요(왜지??)

☆ 2d 렌더타겟 완성

■ 디라이트 구현(리소스에서 구현)
폰트매니저다 만들었음
1. 디라이트 팩토리 생성(필수)
	(IDWriteFactory5)

#실무에서는 폰트캐싱이라고 쉐이더로 그려내서 사용한다.
 우리는 쉽게하려고 디라이트로 했음


//2교시
폰트 클래스 제작
■ 폰트컬렉션 클래스 제작
(폰트 파일 읽어와서 컬렉션 만들어준다.)
폰트랑 세트를 이루는 녀석이라 보면 됨

1. 폰트파일 레퍼런스 생성
2. IDWriteFontSetBulder 필요
3. 빌더 세팅
4. 빌더에 읽어온 폰트파일을 집어넣어준다.
5. 빌더를 이용해 폰트셋을 만들어준다.
6. 그 폰트셋으로 컬렉션을 만들어준다.(폰트콜렉션 프롬 폰트셋)

■폰트클래스 제작
IDWriteTextFormat 필요
폰트 로딩할때 폰트 컬렉션 클래스 넣어서 가져와서 쓸 수 있고
아니면 그냥 별도 세팅해줘서 쓸 수 있다.

CreateTextFormat()
폰트네임,
널
굵기
사이즈
노말(기울임 이탤릭, 밑줄)
자간
로컬경로 이름

-컬렉션을 이용해서 폰트 페이스네임을 얻어올 것
 폰트 패밀리 얻어오기


//아직 구현중
내일 테스트